## Getting started
1. Install Docker Desktop (WSL2 backend on Windows)
2. `cp .env.example .env`
3. `docker compose up --build`
4. Frontend: http://localhost with override it should be on http://localhost:5173/ double check with "docker ps"
5. Swagger: http://localhost:8080